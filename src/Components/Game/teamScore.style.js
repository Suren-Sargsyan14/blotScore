import { StyleSheet } from 'react-native';

import { COLORS } from '../../UI';

export default StyleSheet.create({
  text: {
    color: COLORS.white,
    fontSize: 23,
    marginVertical: 10,
    width: 100,
  },
});
