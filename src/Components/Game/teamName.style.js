import { StyleSheet } from 'react-native';

import { COLORS } from '../../UI';

export default StyleSheet.create({
    teamText: {
        color: COLORS.white,
        fontSize: 23,
        marginVertical: 10,
    },
});
